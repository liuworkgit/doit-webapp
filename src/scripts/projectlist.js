'use strict';
import Project from "./project";

/**
 * @class ProjectList
 * 
 * Represents a list of projects.
 */
class ProjectList {
    /**
     * Constructor for a ProjectList object.
     */
    constructor() {};

    /**
     * Adds a new project.
     * @param {Project} p - Project to be added. 
     */
    addProj(p) {};

    /**
     * Remove the project with the given id.
     * @param {Number} id - The id of the project to remove.
     */
    removeProj(id) {};
};